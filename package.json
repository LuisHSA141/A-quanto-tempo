{
  "name": "projeto141-vercel-timer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-start-time.js",
    "dev": "node scripts/generate-start-time.js && npx serve public"
  },
  "engines": {
    "node": ">=18"
  }
}
